main() {
	int n = getchar() - '0';
	int prev = 1;
	int curr = 1;
	int aux;
	if (n == 1 || n == 2) {curr = 1;}
	else {
		n -= 2;
		while (n >= 0) {
			aux = curr;
			curr += prev;
			prev = aux;
			n--;
		}
	}
	System.out.println("Fibonacci de %d es %d", n, curr);
}
